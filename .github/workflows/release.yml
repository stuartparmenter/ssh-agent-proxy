name: Release

on:
  release:
    types: [published]

permissions:
  contents: write
  id-token: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    environment: ${{ matrix.environment }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            artifact: ssh-agent-proxy
            environment: ''
          - target: aarch64-apple-darwin
            os: macos-latest
            artifact: ssh-agent-proxy
            environment: ''
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            artifact: ssh-agent-proxy.exe
            environment: signing
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9 # stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Cache cargo
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-${{ matrix.target }}-cargo-

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Azure login (OIDC)
        if: runner.os == 'Windows'
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          allow-no-subscriptions: true

      - name: Sign exe (Windows)
        if: runner.os == 'Windows'
        uses: Azure/artifact-signing-action@b443cf8ea4124818d2ea9f043cba29fc3ec47b16 # v1.2.0
        with:
          endpoint: ${{ vars.AZURE_SIGNING_ENDPOINT }}
          signing-account-name: ${{ vars.AZURE_SIGNING_ACCOUNT }}
          certificate-profile-name: ${{ vars.AZURE_SIGNING_PROFILE }}
          files: ${{ github.workspace }}/target/${{ matrix.target }}/release/ssh-agent-proxy.exe

      - name: Setup .NET (Windows)
        if: runner.os == 'Windows'
        uses: actions/setup-dotnet@c2fa09f4bde5ebb9d1777cf28262a3eb3db3ced7 # v5.2.0
        with:
          dotnet-version: '9.x'

      - name: Install WiX (Windows)
        if: runner.os == 'Windows'
        run: dotnet tool install --global wix

      - name: Build MSI (Windows)
        if: runner.os == 'Windows'
        env:
          TARGET: ${{ matrix.target }}
          TAG: ${{ github.event.release.tag_name }}
        shell: pwsh
        run: |
          $version = ($env:TAG -replace '^v', '')
          wix build -acceptEula wix7 `
              -arch x64 `
              -d "Version=$version" `
              -d "SourceExe=$env:GITHUB_WORKSPACE/target/$env:TARGET/release/ssh-agent-proxy.exe" `
              -o "ssh-agent-proxy-$env:TARGET.msi" `
              wix/ssh-agent-proxy.wxs

      - name: Sign MSI (Windows)
        if: runner.os == 'Windows'
        uses: Azure/artifact-signing-action@b443cf8ea4124818d2ea9f043cba29fc3ec47b16 # v1.2.0
        with:
          endpoint: ${{ vars.AZURE_SIGNING_ENDPOINT }}
          signing-account-name: ${{ vars.AZURE_SIGNING_ACCOUNT }}
          certificate-profile-name: ${{ vars.AZURE_SIGNING_PROFILE }}
          files: ${{ github.workspace }}/ssh-agent-proxy-${{ matrix.target }}.msi

      - name: Package (Unix)
        if: runner.os != 'Windows'
        env:
          TARGET: ${{ matrix.target }}
        run: |
          cd target/${TARGET}/release
          tar czf ../../../ssh-agent-proxy-${TARGET}.tar.gz ssh-agent-proxy
          cd ../../..
          sha256sum ssh-agent-proxy-${TARGET}.tar.gz > ssh-agent-proxy-${TARGET}.tar.gz.sha256

      - name: Package (Windows)
        if: runner.os == 'Windows'
        env:
          TARGET: ${{ matrix.target }}
        run: |
          cd target/$env:TARGET/release
          Compress-Archive -Path ssh-agent-proxy.exe -DestinationPath ../../../ssh-agent-proxy-$env:TARGET.zip
          cd ../../..
          (Get-FileHash -Algorithm SHA256 "ssh-agent-proxy-$env:TARGET.zip").Hash.ToLower() + "  ssh-agent-proxy-$env:TARGET.zip" | Out-File -Encoding ascii "ssh-agent-proxy-$env:TARGET.zip.sha256"
          (Get-FileHash -Algorithm SHA256 "ssh-agent-proxy-$env:TARGET.msi").Hash.ToLower() + "  ssh-agent-proxy-$env:TARGET.msi" | Out-File -Encoding ascii "ssh-agent-proxy-$env:TARGET.msi.sha256"

      - name: Upload release assets
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.event.release.tag_name }}
          TARGET: ${{ matrix.target }}
        run: gh release upload "${TAG}" ssh-agent-proxy-${TARGET}.* --clobber
        shell: bash
